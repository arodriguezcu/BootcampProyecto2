server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CarPalacios/BootcampProyecto2.git
          searchPaths: config-data
          default-label: main
